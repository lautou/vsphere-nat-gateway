# vsphere-nat-gateway

## Prerequisites
* vCenter URL and credentials
* In vCenter:
  * a datacenter
  * a VM folder
  * a datastore
  * 2 networks: 1 for external network (internet access) + 1 private network

